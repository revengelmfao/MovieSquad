# Ensure optional dependencies are installed
optional=true
# Force platform-specific packages to be installed
ignore-scripts=true
# Force using JS implementation instead of native modules
rollup_binary_host_mirror=https://unpkg.com/rollup/dist/rollup.js
